#include <stdio.h>
#include <stdlib.h>

int main () {
    int num1, num2, num3;
    printf("Insira a data atual: ");
    scanf("%d %d %d", &num1, &num2, &num3);
    printf("%d / %d / %d \n", num1, num2, num3);
    return 0;
}